# NPM Configuration for Homebridge Plugin Publishing

# Ensure package is published as public
access=public

# Set default tag for publishing
tag=latest

# Enable strict SSL
strict-ssl=true

# Set registry to official npm registry
registry=https://registry.npmjs.org/

# Enable package-lock.json
package-lock=true

# Set save prefix for dependencies
save-prefix=^
